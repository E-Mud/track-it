@import '../../styles/main.styl'
@import '../card/card.styl'

.modal-mask
  position: fixed;
  z-index: 9998;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, .5);
  @extend .flex-container
  @extend .flex-container.center-center

  .modal
    @extend .card
